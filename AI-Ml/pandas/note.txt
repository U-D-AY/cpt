import pandas as pd

1. series (1-d array)
2. dataframes (2-d array)/ labeling will be on rows and columns
operations:
1. reading / writing of data (csv/ json / xls)
2. inserting of data 
3. selecting  of data
4. data manipulation 
5. grouping of data
6. marging / joining 
7. sorting 

Syntax: DataFrame(data)
df.head(n) first n rows
df.tail(n) for last n rows
df.info() summary -df
df.describe() - statistical summary
df.shape() - tuple-rows,columns
df.dtypes() - data type(column)
df.column() - column name

Data cleaning
1. Handling Missing data(NaN)
isna() - Identify missing Value
fillna() - Speific Value
dropna() - Delete missing Value
2. Remove Duplicate Value
duplicated() - checks duplicates
drop_duplicates()
3. Correcting Invalid Data
12/12/20255 dates, ages, cost(-ve)
clip()
4. Strandardizing / Normalizing num/ strings data
str.lower(), str.upper(), str.strip(), str.replace()
5. Data convertion
pd.to_numperics()
pd.to_datetime()
errors = 'coerce'
